"""JuliusCaesar pre-shipped skill libraries.

Pre-shipped skills that need real Python (beyond a SKILL.md prompt) live
under `lib/skills/<name>/`. The CLI shims in `bin/jc-<name>` import the
package and expose the operator-facing commands.
"""
