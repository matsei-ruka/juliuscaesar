# Instance Skills

Create or install agent-owned skills here:

```text
skills/<skillname>/SKILL.md
```

Pre-shipped skills are listed in `skills/Index.md`. Read that index before
choosing a web, search, crawl, or browser automation skill.

This directory belongs to the instance. Use global assistant skill directories
only when the operator explicitly asks for a global skill.
